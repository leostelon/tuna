import 'package:flutter/material.dart';

class BridgeSelect extends StatelessWidget {
  const BridgeSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Bridge"),
      ),
    );
  }
}
